<div id="autocomplete-list" class="autocomplete-items">
    <div class="clickspot">
        <button id="id-picker">{{name}}</button>
        <input id="idVal" type="hidden" value="{{_id}}">
    </div>
</div>